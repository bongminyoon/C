#include <stdio.h>


double fine_max(double* pa, int size);
int main(void)
{
	double ary[] = { 2.5, 3.1, 5.5, 7.1, 4.6, 9.9, 1.5 };
	double max = 0;
	double* a;
	a = ary;
	size = 

	max = double fine_max(double* a, int size);
	printf("배열의 최대값 : %d", max);

}
double fine_max(double* pa, int size)
{
	
	//for (i = 0; i < 8; i++)
	{
		if ( >max)max = 
	}
		
	return max;
	
 }