#include <stdio.h>

int prime_check(int n);


int main(void)
{
	int i, j;
	scanf("%d\n", &j);

	for (i = 0; i < j; i++)
	{
		while (prime_check(i)) // 0이면 x 1이면 o
		{
			printf("%d", i)
		}
	}

}
int prime_check(int n)
{
	int k;
	if (n % k =)
}